name: Update Servers List

on:
  push:
    branches:
      - main
      - master
  schedule:
    - cron: '0 1 * * *'
  workflow_dispatch:

jobs:
  update-servers:
    runs-on: ubuntu-24.04
    
    steps:
    - uses: actions/checkout@v2
      
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytz requests brotli
        
    - name: Configure Git
      run: |
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        git config --global user.name "github-actions[bot]"
        
    - name: Update and commit changes
      run: |
        # 设置临时环境变量，只在执行 Python 脚本时生效
        export http_proxy="http://115.190.178.71:3128"
        export https_proxy="http://115.190.178.71:3128"
        python jetbrains_servers_updater.py
        unset http_proxy https_proxy
        
        git add .
        git diff --quiet && git diff --staged --quiet || (git commit -m "Update servers list" && git push)
      env:
        PASSWORD: ${{ secrets.PASSWORD }}
        USERNAME: ${{ secrets.USERNAME }}
